server:
  port: 8000

spring:
  datasource:
    url: ${JDBC_DATABASE_URL:jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres}
    username: ${DATABASE_USERNAME:postgres.zzbzzfbvyteayasvehpk}
    password: ${DATABASE_PASSWORD:Yashas@042005}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

security:
  jwt:
    secret-key: ${SECRET_KEY:supersecretkey123}
    expire-minutes: ${ACCESS_TOKEN_EXPIRE_MINUTES:30}

gemini:
  api:
    key: ${GEMINI_API_KEY:}
